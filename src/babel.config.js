module.exports = {
    presets: [
      "@babel/preset-env",    // Compiles modern JavaScript down to older versions for compatibility
      "@babel/preset-react"   // Compiles JSX syntax used by React
    ],
    plugins: [
      // Add any additional Babel plugins if needed
    ],
  };
  